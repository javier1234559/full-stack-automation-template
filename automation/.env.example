# Optional: used when automation needs Supabase (setup 3). Copy to .env and fill in.
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Or use same vars as frontend (root .env) if running in same environment:
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
